service chargen
{
    type        = INTERNAL
    id          = chargen-dgram
    socket_type = dgram
    protocol    = udp
    wait        = yes
    user        = nobody
    disable     = no
    flags       = IPv4
}